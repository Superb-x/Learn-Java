flush（刷新）操作，输出流中都有flush方法
计算机访问外部设备（磁盘文件）要比直接访问内存慢得多，如果每次write都要直接写入到磁盘文件中，CPU都会花更多的时间，此时我们可以使用缓冲区来解决这个问题
使用缓冲区的好处：
	1、提高CPU使用率
	2、有机会回滚写入的数据
对于字节流，flush方法不是都有作用（部分字节流才有作用，缓冲流），对于字符流都起作用
		如果我们调用close方法，系统在关闭资源前，会先调用flush方法
----------------------------
操作系统使用-1来表示磁盘的文件结尾
缓冲区大小一般容量的整数倍，可以提高IO性能